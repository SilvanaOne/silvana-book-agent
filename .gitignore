# Rust
target/
to-delete/

# Backups
backups/

# Logs
logs/

# Environment
.env
.env.*
configuration.toml
agent-state.json
tpsl-state.json
dca-state.json
/cloud-agent
/agent-spot-dca
/agent-tpsl

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
plans/

# OS
.DS_Store
Thumbs.db

logs

# Docker build artifacts
docker/release/

# Demo scaffolds — Next.js build/deps live per-demo (covered by each demo's
# own .gitignore too, but this catches empty scaffolds that don't have one yet).
**/demo/node_modules/
**/demo/.next/
**/demo/*.tsbuildinfo

# Generated by scripts/gen-demos-compose.sh from the discovered demos — do not
# commit; the deploy job and local dev both regenerate it on demand.
/docker-compose.demos.yml

# Runtime working directories (contain .env with private key + local fixtures)
runtime/
runtime2/

# Claude Code session data
.claude/
