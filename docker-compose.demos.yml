# GENERATED by scripts/gen-demos-compose.sh — do not edit by hand.
# Runs every agent demo discovered under crates/*/demo/. Add a new demo
# folder with a Dockerfile and it appears here automatically.
services:
  spot-dca-demo:
    image: ${SPOT_DCA_IMAGE:-ghcr.io/silvanaone/spot-dca-demo:${DEMO_TAG:-latest}}
    build:
      context: ./crates/agent-spot-dca/demo
    container_name: spot-dca-demo
    restart: unless-stopped
    ports:
      - "3003:3003"
  tpsl-demo:
    image: ${TPSL_IMAGE:-ghcr.io/silvanaone/tpsl-demo:${DEMO_TAG:-latest}}
    build:
      context: ./crates/agent-tpsl/demo
    container_name: tpsl-demo
    restart: unless-stopped
    ports:
      - "3002:3002"
